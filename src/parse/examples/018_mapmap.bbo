contract A(bool[address][bool] mat)
{
	case (void set(bool x, address y, bool value))
	{
		mat[x][y] = value;
		return then become A(mat);
	}
	case (bool get(bool x, address y))
	{
		return (mat[x][y]) then become A(mat);
	}
}
