

contract auction
  (uint _bidding_time
  ,bool[address] _bids
  ,uint _highest_bid)
{
   case(bool bid())
   {
	bid new_bid =
		new bid(sender(msg), this) along value(msg)
			 reentrance { abort; }; // failure throws.
     return (true) then become
     auction(_bidding_time, _bids, value(msg));
   }
}
contract bid
  (address _sender
  ,address _auction
  )
{
}
