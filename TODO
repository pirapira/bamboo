# todo for the first example
CallExp is too ambiguous
annotate unique ids to sentences
generate a JUMPDEST for each sentence

runtime code generation for auction_done()
deployment code for auction_done()
ABI JSON for auction_done()

runtime code generation for auction() and auction_done()
runtime code generation for bid()


# todo for later
add [msg] as a special token
think what to do with balance checks, block numbers and so on.  block()?
check for duplication of variable names
check for duplication of contract interface function names (even in different contracts)
add a placeholder for arrays in the contract creation e.g. new auction(x, y, empty_array, z)...

# done
parentheses node should be removed early, at typing perhaps
